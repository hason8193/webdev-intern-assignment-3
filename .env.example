# Environment variables for Railway deployment
# Copy this file to .env for local development or set these in Railway dashboard

# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-domain.railway.app,localhost,127.0.0.1,healthcheck.railway.app

# Database Settings (Railway will provide these)
DATABASE_NAME=railway
DATABASE_USER=root
DATABASE_PASSWORD=your-database-password
DATABASE_HOST=your-database-host
DATABASE_PORT=3306

# Optional: For PostgreSQL instead of MySQL
# DATABASE_URL=postgresql://user:password@host:port/database